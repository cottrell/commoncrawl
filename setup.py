from setuptools import setup

setup(name='commoncrawlscraper',
      version='0.0',
      description='crawl small subsets with persistent caching',
      packages=['commoncrawlscraper'],
      zip_safe=False)
